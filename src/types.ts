export type WebsiteData ={
    teachers: {
        name:string,
        description: string,
        image: string
    }[],
    pricing: {
        group:string,
        description:string
    }[]
}
    
